'use client'

import * as d3 from 'd3';
import { FeatureCollection } from 'geojson';

const Map = ({ data }: { data: FeatureCollection }) => {
  return (
    <div>
      map
    </div>
  )
}

export default Map